<!-- doc_id: doc_server_service_0004 -->
# 向量檢索與資料入庫

這份文件描述 standalone `3klife-npc-brain` 的 vector 設定、export、upsert 與 query。所有路徑都以 repo root `C:/Users/User/3klife-npc-brain` 為基準。

## 原則

- Vector DB 不是 canonical truth；canonical truth 仍是 pipeline artifacts / PostgreSQL / JSONB。
- Pinecone / Qdrant 用來補強 recall 與 retrieval。
- Runtime 目前以 artifact-first、vector-second 為主。

## 環境變數

```text
NPC_VECTOR_PROVIDER_ORDER=pinecone,qdrant,sqlite_vec
NPC_VECTOR_DEFAULT_PROVIDER=pinecone
NPC_VECTOR_DIMENSION=1024
NPC_EMBEDDING_PROVIDER=sentence_transformers
NPC_EMBEDDING_MODEL=BAAI/bge-m3

PINECONE_API_KEY=<key>
NPC_PINECONE_INDEX=sanguo-npc-brain-dev

NPC_QDRANT_URL=http://host.docker.internal:6333
NPC_QDRANT_COLLECTION_FACTS=romance_facts_v1
NPC_QDRANT_COLLECTION_KEYWORDS=general_keywords_v1
NPC_QDRANT_COLLECTION_PERSONA=general_persona_v2
```

## Docker 內檢查 vector 設定

```powershell
cd C:/Users/User/3klife-npc-brain
docker exec 3klife-npc-brain-dev python -B -m app.vector_env_smoke_test
```

## 啟動 Qdrant

若使用獨立 Qdrant compose：

```powershell
cd C:/Users/User/3klife-npc-brain
docker compose -f docker-compose.qdrant.yml up -d
```

若從 `docker-compose.dev.yml` 連 Windows 上的 Qdrant，container 內使用：

```text
NPC_QDRANT_URL=http://host.docker.internal:6333
```

## 匯出 vector-ready records

```powershell
cd C:/Users/User/3klife-npc-brain
python pipelines/sanguo-rag/export_vector_records.py `
  --events <ready-eval-events.jsonl> `
  --keyword-root <keyword-options-dir> `
  --persona-root <persona-cards-dir> `
  --general-id sima-yi `
  --output-root scratch/vector-ready-sima-yi `
  --overwrite
```

輸出：

- `vector-records.facts.jsonl`
- `vector-records.keywords.jsonl`
- `vector-records.persona.jsonl`
- `vector-records.all.jsonl`
- `vector-records.index.json`

## Pinecone index

```powershell
cd C:/Users/User/3klife-npc-brain
python pipelines/sanguo-rag/init_pinecone_index.py --dry-run
python pipelines/sanguo-rag/init_pinecone_index.py
```

## Upsert records

```powershell
cd C:/Users/User/3klife-npc-brain
python pipelines/sanguo-rag/upsert_pinecone_records.py `
  --records-root scratch/vector-ready-sima-yi `
  --embedding-provider mock
```

## Query

```powershell
cd C:/Users/User/3klife-npc-brain
python pipelines/sanguo-rag/query_pinecone_records.py `
  --namespace keywords `
  --query-text "司馬懿 與 司馬昭 關係" `
  --embedding-provider mock `
  --top-k 3 `
  --expected-id "keyword::sima-yi::sima-zhao"
```

## Metadata 注意事項

Pinecone metadata 不接受 `null`。寫入前需移除 `None/null`，並只保留 string / number / boolean / list[string]。
