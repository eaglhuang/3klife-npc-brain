<!-- doc_id: doc_server_data_0002 -->
# Rules

Rule 資料描述如何從原文拆解語意，只產生 hint，不直接宣告 canonical fact。

範例 ID：

- `Rule_AddressTitle_SpouseHint`
- `Rule_Speech_DirectAddressee`
- `Rule_Action_ProtectsPerson`

大量 Rule row 使用 JSONL，例如 `rule-address-title-semantics.jsonl`。
